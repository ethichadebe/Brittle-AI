{
  "name": "accucery",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w packages/types && npm run build -w frontend && npm run build -w backend",
    "lint": "eslint .",
    "typecheck": "npm run typecheck -w packages/types && npm run typecheck -w frontend && npm run typecheck -w backend"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^9.1.2",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^16.5.0",
    "typescript-eslint": "^8.70.0"
  }
}
